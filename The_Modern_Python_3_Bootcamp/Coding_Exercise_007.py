# Set the variable called first to your name.
first = "Eric"

# Set the variable called last to your last name.
last = "Meyr"

# Then set the variable called formatted that interpolates both using the
# .format() method.
# Make sure you follow this pattern:
#   "First Name: Colt, Last Name: Steele"

formatted = "First Name: {}, Last Name: {}".format(first, last)

print(formatted)