# Define a list called my_stuff. It must be at least four elements long,
# and must contain at least 1 string and 1 float

my_stuff = [1, 2, "1.0", "2.0"]

# Define a list called nums. It should be a list containing all the
# numbers between 1 and 99. Do not type it out manually, use the
# previous video!

nums = list(range(1,100))